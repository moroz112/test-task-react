import {
	FILTER_CHANGED_SUCCESS
} from 'actionTypes'
const initialState = '';

export default (state = initialState, {type, payload}) => {
	switch (type) {
		case FILTER_CHANGED_SUCCESS:
			return payload;
		default:
			return state
	}
}